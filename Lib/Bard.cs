using System;

namespace PGLib
{
    public class Bard : PG
    {
        public Bard(int str, int dex, int con, int size, int intel, int pow, int ch)
            : base(str, dex, con, size, intel, pow, ch)
        {}
    }
}
